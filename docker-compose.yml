version: "3.9"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ethicalguard
      POSTGRES_USER: ethicalguard
      POSTGRES_PASSWORD: ethicalguard
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
